function createNewProfile(data) {

    var e = ModunUserMan.createNewProfile(data);
    return e;

}

function createNewEntity(data) {
  
  var e = ModuleEntityMana.createNewEntity(data);
  
  return e;

}

function enableEntity(index,identity,status)
{ 
  var e = ModuleEntityMana.enableEntity(index,identity,status);
  
  return e;
}

function saveEntityField(data)
{
  var e = ModuleEntityMana.saveEntityField(data);
  
  return e;
}

function editProfile(data){

  
  
}